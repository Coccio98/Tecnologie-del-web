<div class="input-radio">
    <input type="radio" id="{{$n}}" name="{{$name}}" value="{{$id}}" class="category">
    <label for="{{$n}}">
        <span></span>
        {{$category}}
        <small>({{$slot}})</small>
    </label>
</div>
